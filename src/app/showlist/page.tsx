import { ShowsList } from "@/components/ShowList/ShowList";
import React from "react";

const page = () => {
  return (
    <div className="min-h-screen justify-center items-center">
      <ShowsList />
    </div>
  );
};

export default page;
